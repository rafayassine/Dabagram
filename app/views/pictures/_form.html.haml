= simple_form_for @picture, html: { multipart: true} do |f|
  - if @picture.errors.any?
    #errors
      %h2
      = pluralize(@picture.errors.count, "error")
      prevented this picture from saving
      %ul
        - @picture.errors.full_message.each do |msg|
          %li= msg
  .form-group
    = f.input :title, input_html: { class: 'form-control'}
  .form-group
    = f.input :description, input_html: { class: 'form-control'}
  = f.button :submit, input_html: { class: 'btn btn-alert blue'}
